package loa;

import javax.swing.JLabel;
/** GUI ojbect to display whose turn.
 * @author padentomasello
 *
 */
public class Output extends JLabel {
    /** Board. */
    private Board board;
    /** Creates output Fui for BOARD0 and BOX msg. */
    Output(Board board0, QueuedMailBox box) {
        super();
        board = board0;
    }
    /** Contructor with no args. */
    Output() {
        super();
    }
    /** Contructor for STRING. */
    Output(String string) {
        super(string);
    }

}
